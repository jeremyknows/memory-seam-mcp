# memory-seam-mcp — moved

**This bridge now lives in the main memory-seam repository:**
https://github.com/jeremyknows/memory-seam (under `bridge/`)

Install:

```bash
pip install "git+https://github.com/jeremyknows/memory-seam.git#subdirectory=bridge"
```

This standalone repository is archived. All development, issues, and releases happen in the main repo.
